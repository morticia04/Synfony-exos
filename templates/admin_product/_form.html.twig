{{ form_start(form) }}

    {{ form_row(form.titre) }}    

     {{ form_row(form.images) }}   

    {{ form_row(form.slug) }}    

    {{ form_row(form.tva) }}

    {{ form_row(form.Prix) }}
    <button class="btn">{{ button_label|default('Save') }}</button>

{{ form_end(form) }}
